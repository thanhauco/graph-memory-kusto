apiVersion: v1
kind: Namespace
metadata:
  name: icm-memory
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: neo4j, namespace: icm-memory }
spec:
  replicas: 1
  selector: { matchLabels: { app: neo4j } }
  template:
    metadata: { labels: { app: neo4j } }
    spec:
      containers:
        - name: neo4j
          image: neo4j:5.19
          env:
            - { name: NEO4J_AUTH,    value: "neo4j/neo4jpass" }
            - { name: NEO4J_PLUGINS, value: '["graph-data-science"]' }
          ports: [ { containerPort: 7687 }, { containerPort: 7474 } ]
---
apiVersion: v1
kind: Service
metadata: { name: neo4j, namespace: icm-memory }
spec:
  selector: { app: neo4j }
  ports:
    - { name: bolt, port: 7687, targetPort: 7687 }
    - { name: http, port: 7474, targetPort: 7474 }
